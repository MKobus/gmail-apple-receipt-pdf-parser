<div class="container" style="margin-top: 100px;">
	<div class="row">
	<h3 style="margin-bottom:40px;">Please pick dates to search for duplicates, or submit and search entire database</h3>
		<div class="col-xs-12 col-sm-8 col-sm-offset-2">
			<div class="input-append date col-xs-6" id="dp1" data-date-format="yyyy-mm-dd">
			  <label class="col-xs-4">From:</label><input class="col-xs-8 datepicker begin_date" size="16" type="text" value="">
			  <span class="add-on"><i class="icon-th"></i></span>
			</div>

			<div class="input-append date col-xs-6" id="dp2" data-date-format="yyyy-mm-dd">
			  <label class="col-xs-4">To:</label><input class="col-xs-8 datepicker end_date" size="16" type="text" value="">
			  <span class="add-on"><i class="icon-th"></i></span>
			</div>

			<button type="button" class="btn btn-info col-xs-6 col-xs-offset-3" style="margin-top:40px;">Check Database for Duplicates</button>
		</div>
	</div>
</div>




<script type="text/javascript">
 $('.datepicker').datepicker({
 	format: 'yyyy-mm-dd'
 })


  var nowTemp = new Date();
	var now = new Date(nowTemp.getFullYear(), nowTemp.getMonth(), nowTemp.getDate(), 0, 0, 0, 0);


	$("button").click(function(){
		begin_date = $("input.begin_date").val()
		end_date = $("input.end_date").val()
		 testDates();
	})

	 function testDates() {

      var from = $(".begin_date").val();
      var to = $(".end_date").val();

      console.log(from)
     if(to.length < 0){
     	 if (from > to) {
          alert("End date is greater than Begin Date!");
          return;
      }else{
      	params = {start_date: from, end_date: to}
      	console.log(params)
      	str = $.param(params)
      	console.log(str)
      	window.location = "/emails?"+str
      	console.log(check)
      }
    }else{
    	params = {start_date: from}
      	console.log(params)
      	str = $.param(params)
      	console.log(str)
      	window.location = "/emails?"+str
      	console.log(check)
      }
    }

</script>